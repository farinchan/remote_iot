import 'package:flutter/material.dart';

class BluetoohPage extends StatelessWidget {
  const BluetoohPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bluetooth Page"),
      ),
    );
  }
}